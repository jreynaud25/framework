{
  "name": "@framework/plugin",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "FW_BUILD_TARGET=code vite build && FW_BUILD_TARGET=ui vite build",
    "dev": "FW_BUILD_TARGET=code vite build --watch",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint configured'",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@framework/types": "workspace:*"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.108.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vite-plugin-singlefile": "^2.1.0"
  }
}
